__pycache__/
*.py[cod]
.pytest_cache/
.venv/
build/
dist/
build_log.txt
*.egg-info/
tests/fixtures/*.json

# App antigo (captura de tela). Movido para cá inteiro em vez de apagado:
# se algum dia fizer falta, está tudo lá. Pode apagar a pasta à vontade.
_legado/

# Atalho gerado por tools/criar-atalho.ps1 (aponta para o pythonw desta máquina)
*.lnk
dist-standalone/

# Amostras de voz geradas por tools/comparar_vozes.py (regeraveis, nao versionar)
docs/amostras-voz/

# Chave privada Ed25519 do servidor de licencas. NUNCA versionar:
# quem tem este arquivo emite licenca de graca para sempre.
chaves-licenca/
*.chave-privada
servidor/.env
servidor/licencas.db*
